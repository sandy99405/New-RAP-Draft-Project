import type { MemFsEditor } from '../index.ts';
import type { WriteOptions } from './write.ts';
type JSONReplacer = ((this: unknown, key: string, value: unknown) => unknown) | (number | string)[] | null;
export default function writeJSON(this: MemFsEditor, ...[filepath, contents, replacer, space, options]: [
    filepath: string,
    contents: unknown,
    replacer?: JSONReplacer,
    space?: string | number,
    options?: WriteOptions
]): string;
export {};
