import type fs from 'node:fs';
import type { MemFsEditor, MemFsEditorFile } from '../index.ts';
import type { Store } from 'mem-fs';
type CompareFile = {
    contents: null | Buffer;
    stat?: {
        mode?: number;
    } | null;
};
export declare const isMemFsEditorFileEqual: (a: CompareFile, b: CompareFile) => boolean;
export declare function writeInternal<EditorFile extends MemFsEditorFile>(store: Store<EditorFile>, file: EditorFile): void;
export type WriteOptions = {
    stat?: fs.Stats;
    metadata?: Record<string, unknown>;
};
/**
 * @deprecated Pass a `WriteOptions` object instead: `write(filepath, contents, { stat })`.
 */
export default function write(this: MemFsEditor, filepath: string, contents: string | Buffer, stat: fs.Stats): string;
export default function write(this: MemFsEditor, filepath: string, contents: string | Buffer, options?: WriteOptions): string;
export {};
